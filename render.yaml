services:
  - type: web
    name: eleitoral-inteligencia-alocacao
    runtime: python
    buildCommand: pip install -r requirements.txt && python data/gerar_demo.py
    startCommand: python run.py
    envVars: []
    healthCheckPath: /api/health
